# Public site URL (used in metadata, sitemap, OG tags). Set to your deployed URL.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional: notify this email on each new contact submission (logged on server)
CONTACT_NOTIFY_EMAIL=prasiddhaf23@gmail.com
